"""Bootstrap login tests — removed in M10a.

The bootstrap login endpoint (DEV-only, returned static API_TOKEN) was removed
in M10a. Comprehensive auth tests now live in tests/auth/test_auth_endpoints.py.
"""

# No tests — file kept as tombstone to avoid confusion.
